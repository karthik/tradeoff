`ro cache=TRUE, tidy=FALSE, warning=FALSE, message=FALSE, refresh=2 or`

``` {r  echo=FALSE }
options(device = function(width = 5, height = 5) {
    pdf(NULL, width = width, height = height)
})
````

# Life-history tradeoffs trial runs
### Karthik Ram
 `r date()`


Clear the workspace and load package dependencies:
``` {r  setup, echo=FALSE}
rm(list=ls())
require(ggplot2)
require(plyr)
library(varDev)
source('tradeoff_functions.r')
````

```{r}
a <- 0.9
b <- -0.8
sA <- 0.7 # adult mortality
Fec <- 2 #fecundity
simple <- tradeoff(a, b, sA)
vd_test <- vd_tradeoff(a, b, sA)
simple
vd_test
````
